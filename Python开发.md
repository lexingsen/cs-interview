### Python开发

```shell
pip list # 查看已经安装的包
pip freeze > requirements.txt # 打包到文件
pip install -r requirements.txt

pip config edit --editor notepad
pip config list -v
pip config set x.y z

```

